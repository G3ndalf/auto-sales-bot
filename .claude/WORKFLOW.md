# WORKFLOW.md — Пайплайн разработки

## Правило №1: Никакой коммит без ревью

Перед `git commit` ВСЕГДА прогоняй diff через чеклист. Ни одна строчка не уходит в прод без проверки.

## Пайплайн по типу задачи

### 🔧 Новая фича
1. **Планирование** — определи какие файлы и модули затронуты
2. **Разработка** — используй нужного агента:
   - Python backend → `bot-developer`
   - Mini App (React/TS) → `mini-app-dev`
   - БД/модели/миграции → `db-architect`
3. **Ревью** → `code-reviewer` — прогони `git diff` через чеклист
4. **Тесты** → `tester` — если есть тесты, запусти; если нет — напиши
5. **Коммит** — только после прохождения шагов 3-4
6. **Деплой** → `deploy-manager` — сборка + деплой + проверка логов

### 🐛 Баг-фикс
1. **Диагностика** → `debugger` — анализ трейсбека и логов
2. **Фикс** — минимальное исправление
3. **Ревью** → `code-reviewer` — проверка фикса
4. **Коммит + деплой**
5. **Проверка логов** — убедись что ошибка больше не появляется

### 🔒 Безопасность
- `security-auditor` — запускай после крупных изменений или перед релизом
- Отдельно проверяй: токены, SQL, auth, CORS, input validation

### 📖 Документация
- `docs-writer` — после крупных фич обнови README/CLAUDE.md

## Чеклист code-reviewer (минимальный, перед каждым коммитом)

### Python
- [ ] Все аргументы функций совпадают с сигнатурами (типы, nullable)
- [ ] NOT NULL поля в БД — не передаём None
- [ ] Новые импорты добавлены
- [ ] async/await не пропущены
- [ ] session.commit() вызывается (или через middleware)
- [ ] Пользовательские данные экранируются (html.escape)
- [ ] Нет хардкода токенов/паролей

### TypeScript/React
- [ ] API вызовы обрабатывают ошибки (try/catch)
- [ ] Типы совпадают с серверным API
- [ ] Нет обращений к undefined свойствам
- [ ] URL параметры правильно кодируются

### Деплой
- [ ] `git diff` — посмотреть что именно уходит
- [ ] Vite build успешен (нет ошибок компиляции)
- [ ] Логи после рестарта — нет ошибок первые 10 секунд
- [ ] Функционал проверен (запрос /start, открытие Mini App)

## Когда пропустить ревью? НИКОГДА.

Даже "просто CSS" или "одна строчка" — прогони через чеклист.
Баг с `full_name=None` — это была "одна строчка" которая сломала весь submit.
