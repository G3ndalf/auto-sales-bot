# Code Review — Auto Sales Bot

## Дата: 2026-02-05
## Ревьюер: Claude Code (автоматический аудит)

---

### 🔴 Критические (нужно исправить СЕЙЧАС)

#### 1. IDOR: Отсутствует аутентификация пользователей — любой может редактировать/удалять чужие объявления
**Файл:** `app/api.py`, строки edit/delete эндпоинтов (~440-620)  
**Проблема:** Эндпоинты `PUT /api/ads/car/{ad_id}?user_id=<telegram_id>` и `DELETE /api/ads/car/{ad_id}?user_id=<telegram_id>` принимают `user_id` как query parameter **без какой-либо проверки подлинности**. Злоумышленник может подставить чужой `telegram_id` и редактировать/удалять любые объявления.  
**Также затронуты:**
- `GET /api/user/{telegram_id}/ads` — просмотр всех объявлений любого пользователя
- `GET /api/profile/{telegram_id}` — просмотр профиля любого пользователя
- `POST /api/submit` — создание объявлений от имени любого пользователя  

**Рекомендация:** Внедрить верификацию `initData` через HMAC-подпись Telegram (см. [Telegram Web App Validation](https://core.telegram.org/bots/webapps#validating-data-received-via-the-mini-app)). Все мутирующие эндпоинты должны проверять подпись.

---

#### 2. Админ-авторизация обходится подменой user_id
**Файл:** `app/api.py`, функция `_check_admin_access()`, строки ~60-95  
**Проблема:** Метод 2 проверки (user_id из header/query) позволяет **любому** получить админ-доступ, просто установив заголовок `X-Telegram-User-Id: 5849807401` или параметр `?user_id=5849807401`. Никакой криптографической верификации нет.  
**Воздействие:** Полный доступ к модерации — одобрение/отклонение объявлений, просмотр статистики.  
**Рекомендация:** Удалить метод 2 (user_id); оставить только токен (`admin_token`) + внедрить верификацию initData.

---

#### 3. CORS: `Access-Control-Allow-Origin: *` — полностью открытый
**Файл:** `app/api.py`, middleware `cors_middleware`, строка ~103  
**Проблема:** Любой сайт может отправлять запросы к API. В сочетании с отсутствием аутентификации (п.1) это позволяет CSRF-атаки с произвольных доменов.  
**Рекомендация:** Ограничить `Access-Control-Allow-Origin` до `https://auto.xlmmama.ru`.

---

#### 4. Сервис работает от root
**Файл:** сервер — `/etc/systemd/system/auto-sales-bot.service`, строка `User=root`  
**Проблема:** Бот и API-сервер работают с правами суперпользователя. При эксплуатации любой уязвимости атакующий получает полный контроль над сервером.  
**Рекомендация:** Создать выделенного пользователя (`useradd -r -s /sbin/nologin autosales`) и запускать сервис от него.

---

#### 5. Нет бэкапов базы данных
**Файл:** сервер — `crontab -l` пуст, нет скриптов бэкапа  
**Проблема:** При сбое диска, ошибке или атаке все данные будут потеряны безвозвратно.  
**Рекомендация:** Добавить cron-job с `pg_dump`:
```bash
0 3 * * * pg_dump -U botuser auto_sales_bot | gzip > /opt/backups/db_$(date +\%Y\%m\%d).sql.gz
```

---

#### 6. Auto-approve: объявления публикуются без модерации
**Файл:** `app/handlers/photos.py`, функция `_finish_and_publish()`, строки ~59-82  
**Проблема:** После загрузки фото (или нажатия «Пропустить») объявление автоматически получает статус `APPROVED` и публикуется в канал — **полностью минуя модерацию**. Любой пользователь может опубликовать что угодно.  
```python
ad.status = AdStatus.APPROVED  # строка ~72
```
**Воздействие:** Спам, неприемлемый контент, мошенничество в публичном канале.  
**Рекомендация:** Убрать auto-approve; оставить статус `PENDING` и публиковать только после ручной модерации.

---

#### 7. Секреты в `.env` на продакшн-сервере доступны через root-сервис
**Файл:** сервер — `/opt/auto-sales-bot/.env`  
**Проблема:** Файл содержит `BOT_TOKEN`, `DATABASE_URL` (с паролем `autokbr2026`), `ADMIN_TOKEN`. Сервис от root имеет доступ ко всему. Локальный `.env` также содержит реальный BOT_TOKEN (хотя в `.gitignore` — хорошо).  
**Рекомендация:** Использовать system secrets management (systemd `LoadCredential`, HashiCorp Vault, или хотя бы `chmod 600`).

---

### 🟡 Важные (нужно исправить скоро)

#### 8. SQL ILIKE wildcard injection
**Файл:** `app/api.py`, `get_car_ads()` и `get_plate_ads()`  
**Проблема:** Поисковый параметр `q` передаётся в ILIKE без экранирования спецсимволов `%` и `_`:
```python
q_pattern = f"%{q}%"  # строка ~168
```
Пользователь может ввести `%` или `____` и получить непредсказуемые результаты, или замедлить БД.  
**Рекомендация:** Экранировать `%` → `\%`, `_` → `\_` перед подстановкой.

---

#### 9. Новая HTTP-сессия на каждый запрос фото (утечка ресурсов)
**Файл:** `app/api.py`, `proxy_photo()`, строка ~310  
**Проблема:** Для каждого запроса фото создаётся новый `aiohttp.ClientSession()`. Это нарушает рекомендации aiohttp (одна сессия на приложение) и может привести к исчерпанию коннектов.  
**Рекомендация:** Создать одну `ClientSession` при старте приложения и переиспользовать.

---

#### 10. Race condition в `get_or_create_user`
**Файл:** `app/services/user_service.py`, строки ~10-25  
**Проблема:** Между `SELECT` и `INSERT` другой запрос может создать того же пользователя, что вызовет `IntegrityError` на уникальном индексе `telegram_id`.  
**Рекомендация:** Обернуть в `try/except IntegrityError` с повторным `SELECT`, или использовать `INSERT ... ON CONFLICT DO UPDATE`.

---

#### 11. Фото-аплоад без привязки к объявлению (orphan files)
**Файл:** `app/api.py`, `handle_photo_upload()`, строки ~490-545  
**Проблема:** Эндпоинт `POST /api/photos/upload` сохраняет файл на диск, но не привязывает к конкретному объявлению. Если пользователь загрузит фото и не создаст объявление — файл останется навечно.  
**Рекомендация:** Добавить периодическую очистку orphan-фото или привязывать загрузку к сессии/черновику.

---

#### 12. API слушает на 0.0.0.0:8080 — доступен без TLS
**Файл:** `app/config.py`, строка `api_host: str = "0.0.0.0"`  
**Проблема:** API-сервер доступен напрямую по HTTP:8080 в обход nginx/TLS. Любой, кто знает IP сервера, может обратиться к API без шифрования.  
**Рекомендация:** Слушать только на `127.0.0.1` — nginx уже проксирует.

---

#### 13. `_FILE_ID_RE` не защищает от path traversal полностью
**Файл:** `app/api.py`, строка ~67, и `app/utils/photo_storage.py`, `get_photo_path()`  
**Проблема:** Regex `[A-Za-z0-9_\-]+` достаточно строгий для Telegram file_id, но в `get_photo_path()` UUID подставляется напрямую в путь без дополнительной проверки `..` или `/`. Текущая реализация безопасна благодаря regex, но нет defense-in-depth.  
**Рекомендация:** Добавить проверку `resolved_path.is_relative_to(UPLOAD_DIR)` после построения пути.

---

#### 14. Frontend: Hook вызывается условно (нарушение Rules of Hooks)
**Файл:** `webapp/src/pages/CarsList.tsx`, строка ~20; `webapp/src/pages/PlatesList.tsx`, строка ~19  
**Проблема:**
```typescript
if (!embedded) useBackButton('/catalog')  // условный вызов хука
```
React хуки **нельзя** вызывать условно — это нарушает правила React и может привести к непредсказуемому поведению.  
**Рекомендация:** Вызывать хук всегда, но передавать `null`/`undefined` когда не нужен.

---

#### 15. Frontend: Debounce-таймер не очищается при unmount
**Файл:** `webapp/src/pages/CarsList.tsx`, строки ~77-90; `webapp/src/pages/PlatesList.tsx`, строки ~68-80  
**Проблема:** `searchTimeout.current` создаётся через `setTimeout`, но нет `useEffect` cleanup. При размонтировании компонента таймер сработает и попытается обновить состояние unmounted-компонента.  
**Рекомендация:**
```typescript
useEffect(() => {
  return () => { if (searchTimeout.current) clearTimeout(searchTimeout.current) }
}, [])
```

---

#### 16. Frontend: `_cachedUid` никогда не сбрасывается
**Файл:** `webapp/src/api.ts`, строки ~130-155  
**Проблема:** Модульная переменная `_cachedUid` кэширует user_id навсегда. Если Mini App перезагрузится с другим пользователем (теоретически возможно), кэш вернёт старое значение.  
**Рекомендация:** Привязать кэш к `sessionStorage` или сбрасывать при изменении URL.

---

#### 17. XSS через `showGlobalError` в `main.tsx`
**Файл:** `webapp/src/main.tsx`, строки ~28-40  
**Проблема:** Функция `showGlobalError` использует `innerHTML` для отображения сообщения об ошибке. Если сообщение ошибки содержит HTML-код (например из пользовательского ввода), это может привести к XSS.
```typescript
div.innerHTML = `<strong>⚠️ JS Error:</strong><br/>${message}...`  // строка ~34
```
**Рекомендация:** Использовать `textContent` вместо `innerHTML` для сообщения.

---

#### 18. Admin token передаётся в URL (лог-утечка)
**Файл:** `app/handlers/start.py`, `_webapp_url()`, строка ~67; `webapp/src/api.ts`, `adminQueryParams()`  
**Проблема:** `admin_token` передаётся как query parameter в URL (`?token=HM0jsVR...`). URL-ы записываются в логи nginx, историю браузера, Telegram серверы. Токен может утечь.  
**Рекомендация:** Передавать токен в заголовке (`Authorization: Bearer ...`), а не в URL.

---

#### 19. Отсутствует Rate Limiting на публичных GET-эндпоинтах
**Файл:** `app/api.py`, все GET-эндпоинты  
**Проблема:** Rate limiter применяется только к `/api/submit` и `/api/photos/upload`. Публичные эндпоинты (`/api/cars`, `/api/brands`, `/api/photos/{file_id}`) не защищены — DoS-атака через массовые запросы.  
**Рекомендация:** Добавить rate limiting на nginx уровне (`limit_req_zone`) или на уровне приложения.

---

### 🟢 Рекомендации (улучшения)

#### 20. Dead code в сервисных функциях
**Файл:** `app/services/car_ad_service.py` — функции `get_approved_car_ads()`, `get_brands()`, `get_models_by_brand()`, `count_approved_by_brand()`  
**Проблема:** Эти функции **не используются нигде** — API-эндпоинты в `api.py` делают запросы напрямую.  
**Рекомендация:** Удалить неиспользуемый код или рефакторить API для использования сервисного слоя.

---

#### 21. Dead code в `app/utils/notify.py`
**Файл:** `app/utils/notify.py`  
**Проблема:** Дублирует `_moderation_keyboard` и `_format_car_ad`/`_format_plate_ad` из `app/handlers/admin.py`. Функция `notify_admins()` не вызывается нигде в текущем коде (notifications происходят напрямую в хендлерах).  
**Рекомендация:** Удалить или консолидировать с `admin.py`.

---

#### 22. Дублирование кода: edit/delete эндпоинты
**Файл:** `app/api.py`, строки ~440-620  
**Проблема:** `edit_car_ad_endpoint` и `edit_plate_ad_endpoint` почти идентичны (~80% общего кода). То же для `delete_car_ad_endpoint` и `delete_plate_ad_endpoint`.  
**Рекомендация:** Извлечь общую логику в helper-функцию с параметризацией по модели.

---

#### 23. Дублирование кода: inline styles в React-компонентах
**Файл:** `webapp/src/pages/MyAds.tsx`, `webapp/src/pages/Profile.tsx`  
**Проблема:** Огромные блоки inline-стилей (по 200+ строк), которые дублируют друг друга и `App.css`. Это затрудняет поддержку и увеличивает бандл.  
**Рекомендация:** Вынести повторяющиеся стили в CSS-классы.

---

#### 24. Нет типизации для `ad` параметра в utility-функциях
**Файлы:** `app/utils/notify.py`, `app/utils/publish.py`, `app/handlers/admin.py`  
**Проблема:** Параметр `ad` типизирован как `Any` (нет аннотации), что снижает проверяемость кода.  
**Рекомендация:** Использовать `Union[CarAd, PlateAd]` или создать Protocol/ABC.

---

#### 25. Магическое число `2027` в валидаторе
**Файл:** `app/utils/validators.py`, строка ~63  
**Проблема:** Максимальный год `2027` захардкожен. Через год придётся обновлять.  
**Рекомендация:** Использовать `datetime.now().year + 1`.

---

#### 26. `FUEL_TYPES` в фронте не содержит "газ"
**Файл:** `webapp/src/constants/texts.ts`, `FUEL_TYPES`  
**Проблема:** Бэкенд поддерживает `FuelType.GAS = "газ"`, но фронтенд не показывает этот вариант в select. Пользователь не может выбрать "газ" как тип топлива (только через чекбокс ГБО).  
**Рекомендация:** Добавить `{ value: 'газ', label: '⛽ Газ (ГБО)' }` или убрать enum из бэкенда.

---

#### 27. Отсутствует nginx rate limiting
**Файл:** сервер — `/etc/nginx/sites-enabled/auto-sales-bot`  
**Проблема:** Нет `limit_req_zone` для защиты от brute-force и DDoS.  
**Рекомендация:**
```nginx
limit_req_zone $binary_remote_addr zone=api:10m rate=30r/s;
location /api/ {
    limit_req zone=api burst=50 nodelay;
    proxy_pass http://127.0.0.1:8080;
}
```

---

#### 28. Отсутствуют security headers в nginx
**Файл:** сервер — `/etc/nginx/sites-enabled/auto-sales-bot`  
**Проблема:** Нет заголовков `X-Frame-Options`, `X-Content-Type-Options`, `Content-Security-Policy`, `Strict-Transport-Security`.  
**Рекомендация:** Добавить стандартные security headers.

---

#### 29. Frontend: нет обработки ошибок для сетевых запросов в каталоге
**Файлы:** `webapp/src/pages/CarsList.tsx`, `webapp/src/pages/PlatesList.tsx`  
**Проблема:** В `catch` блоках `loadAds()` просто игнорируются ошибки (`// ignore`). Пользователь не видит сообщения об ошибке и думает, что "нет объявлений".  
**Рекомендация:** Показывать error state с кнопкой "Повторить".

---

#### 30. `PhotoUploader`: сравнение по ссылке для обновления состояния
**Файл:** `webapp/src/components/PhotoUploader.tsx`, строки ~67-78  
**Проблема:** `prev.map(p => p === tempItem ? ...)` сравнивает объекты по ссылке. Это работает в текущем коде (tempItem из замыкания), но хрупко — при любом рефакторинге сломается.  
**Рекомендация:** Использовать уникальный id (например `crypto.randomUUID()`) для каждого PhotoItem.

---

#### 31. Отсутствуют тесты
**Проблема:** В проекте нет ни одного теста (`pytest`, `vitest`), несмотря на `pytest` в dev-зависимостях. Нет CI/CD pipeline.  
**Рекомендация:** Добавить хотя бы smoke-тесты для критичных эндпоинтов (submit, admin auth, edit/delete).

---

#### 32. Нет `client_max_size` в nginx для upload-прокси
**Файл:** сервер — nginx конфиг  
**Проблема:** Nginx по умолчанию ограничивает body до 1MB, но приложение принимает до 10MB (`client_max_size=10*1024*1024`). Загрузка фото >1MB будет rejected nginx'ом до того, как попадёт в приложение.  
**Рекомендация:** Добавить `client_max_body_size 10m;` в блок `location /api/`.

---

### ✅ Что сделано хорошо

1. **HTML escaping в Telegram-сообщениях** — `html.escape()` корректно используется во всех местах форматирования пользовательских данных (`admin.py`, `notify.py`, `publish.py`, `start.py`). XSS через Telegram исключён.

2. **ORM без raw SQL** — весь доступ к БД через SQLAlchemy ORM с параметризованными запросами. SQL injection невозможен.

3. **Серверная валидация** — полноценные валидаторы в `validators.py` для обоих типов объявлений с проверкой типов, длин и диапазонов. Не доверяют фронтенду.

4. **Rate limiting** — грамотный sliding-window rate limiter с автоочисткой в `rate_limiter.py`. Ограничивает 3 объявления/5 мин и 10/час.

5. **DB сессии правильно управляются** — `DbSessionMiddleware` автоматически коммитит/откатывает. `async with pool()` корректно закрывает сессии.

6. **Error Boundary в React** — глобальный перехватчик ошибок + window.onerror + unhandledrejection. Пользователь всегда видит ошибку вместо белого экрана.

7. **Константы вынесены** — нет магических чисел в бизнес-логике. Все лимиты в `constants.py` и `config.ts`.

8. **Sanitized file_id** — regex `[A-Za-z0-9_\-]+` с ограничением длины (256) для file_id в proxy_photo. Валидация типа и размера при upload.

9. **HashRouter** — правильное решение для Telegram iOS WebView, где BrowserRouter ломается.

10. **Cache-busting URL** — `?v={timestamp}` предотвращает кэширование старого WebApp в Telegram.

11. **Soft delete** — объявления не удаляются физически, а получают статус `REJECTED` с причиной. Это позволяет восстановление и аналитику.

12. **Lazy loading страниц** — React.lazy() для всех route-компонентов уменьшает initial bundle.

13. **Повторная модерация при редактировании** — если approved объявление редактируется, статус сбрасывается на `PENDING`. Предотвращает злоупотребление через edit.

14. **FSM overwrite protection** — `web_app.py` проверяет и очищает предыдущее FSM-состояние перед созданием нового объявления.

15. **Миграции через Alembic** — правильная система управления схемой БД.

---

### 📊 Статистика

| Метрика | Значение |
|---------|----------|
| **Файлов проверено** | 52 |
| **Строк кода** | ~9 790 |
| **Backend (Python)** | ~4 200 строк, 24 файла |
| **Frontend (TS/TSX)** | ~4 600 строк, 21 файл |
| **CSS** | ~990 строк, 2 файла |
| **Инфраструктура** | nginx, systemd, .env |
| 🔴 **Критических** | 7 |
| 🟡 **Важных** | 12 |
| 🟢 **Рекомендаций** | 13 |

---

### 🎯 Приоритеты исправления

**Неделя 1 (ASAP):**
- [ ] 🔴 #1 — Внедрить initData HMAC верификацию
- [ ] 🔴 #2 — Убрать user_id из админ-авторизации
- [ ] 🔴 #3 — Ограничить CORS до `auto.xlmmama.ru`
- [ ] 🔴 #4 — Создать выделенного пользователя для сервиса
- [ ] 🔴 #6 — Убрать auto-approve, оставить модерацию

**Неделя 2:**
- [ ] 🔴 #5 — Настроить бэкапы БД
- [ ] 🔴 #7 — Ограничить доступ к `.env`
- [ ] 🟡 #12 — Слушать на `127.0.0.1` вместо `0.0.0.0`
- [ ] 🟡 #17 — Исправить XSS в `showGlobalError`
- [ ] 🟡 #18 — Перенести admin_token в заголовок

**Неделя 3-4:**
- [ ] 🟡 #8-11, #14-16, #19 — остальные важные исправления
- [ ] 🟢 Рекомендации по улучшению кода
